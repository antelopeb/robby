import os

phrase = ["clear", "clean yourself up"]

# this is written specifically for Mac OSX or Linux
# implementation would have to change to become web
# based or to run on a windows machine
def main(q, prompt) :
    os.system('clear')